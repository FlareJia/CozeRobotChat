# 核心网络请求库
requests>=2.28.1

# 音频设备交互
pyaudio>=0.2.13

# 环境变量管理
python-dotenv>=0.21.0

# 重试机制
retrying>=1.3.3

# 定时任务调度
apscheduler>=3.10.0

# 系统工具库 (Mac/Linux可能需要)
portaudio>=19.6.0  # pyaudio的底层依赖

# 开发工具 (可选)
python-dotenv>=0.21.0  # 已包含，再次强调重要性
loguru>=0.7.0          # 更友好的日志工具（可选替换logging）


filelock>=3.12.2
psutil>=5.9.4
playsound>=1.3.0
